<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Conteúdo</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 p-4">
    <h1 class="text-2xl font-bold mb-4">Editar Conteúdo</h1>
    <form action="/admin/conteudos/<%= conteudo.id %>/edit" method="POST" enctype="multipart/form-data">
        <label class="block mb-2">
            Título:
            <input type="text" name="titulo" class="border rounded p-2 w-full" value="<%= conteudo.titulo %>" required>
        </label>
        <label class="block mb-2">
            Descrição:
            <textarea name="descricao" class="border rounded p-2 w-full" required><%= conteudo.descricao %></textarea>
        </label>
        <label class="block mb-2">
            Imagem Atual:
            <img src="/uploads/<%= conteudo.imagem %>" alt="<%= conteudo.titulo %>" class="h-16 mb-2">
            <input type="hidden" name="imagemAtual" value="<%= conteudo.imagem %>">
        </label>
        <label class="block mb-2">
            Nova Imagem (opcional):
            <input type="file" name="imagem" class="border rounded p-2 w-full">
        </label>
        <button type="submit" class="bg-blue-500 text-white rounded p-2 mt-4">Salvar Alterações</button>
    </form>
</body>
</html>
